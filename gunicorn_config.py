# gunicorn_config.py

bind = '127.0.0.1:8000'  # Bind to localhost on port 8000 or as per your configuration
workers = 3  # Number of worker processes
# You can add more configurations here as needed
